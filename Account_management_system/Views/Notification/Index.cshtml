@model IEnumerable<Account_management_system.Models.tbl_notification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.notification)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_income_category.category_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.notification)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_income_category.category_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.notification_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.notification_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.notification_id })
        </td>
    </tr>
}

</table>
